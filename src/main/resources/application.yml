# TODO: create DEV profile with h2 configured but set production config as a default
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:keys/public.pem
  datasource:
    username: root
    password: mypass
    url: jdbc:mariadb://localhost:3306/dance
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: on
    defer-datasource-initialization: on
  sql:
    init:
      mode: always
  mail:
    username: sadeghabedi36@gmail.com
    password: ktiltuheafahorli
    host: smtp.gmail.com
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
keys:
  pri: classpath:keys/private.pem
  pub: classpath:keys/public.pem
logging:
  level:
    root: debug
server:
  port: 8085