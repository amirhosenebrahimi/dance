# TODO: create DEV profile with h2 configured but set production config as a default
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:keys/public.pem
  datasource:
    username: ${DB_NAME:dance}
    password: ${DB_PASS:PZNH7rF78wE}
    url: ${DB_URL:jdbc:mariadb://localhost/dance}
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: on
    defer-datasource-initialization: on
  sql:
    init:
      mode: always
  mail:
    username: sadeghabedi36@gmail.com
    password: ktiltuheafahorli
    host: smtp.gmail.com
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  profiles:
    active: prod
keys:
  pri: classpath:keys/private.pem
  pub: classpath:keys/public.pem
logging:
  level:
    root: debug